# Optional. Leave unset for local development if the backend runs on port 8787
# on the same hostname as the frontend dev server.
#
# Local explicit example:
# VITE_API_BASE_URL=http://127.0.0.1:8787
#
# Same-origin production example behind Nginx:
VITE_API_BASE_URL=/api

